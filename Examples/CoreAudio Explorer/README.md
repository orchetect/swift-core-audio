# CoreAudio Explorer

This example project demonstrates usage of the `AudioObjectSnapshot` data model type.

> [!NOTE]
> 
> This type is useful in capturing Core Audio state for debugging or end-user bug reporting purposes only.
>
> It is not meant to be used in production to read Core Audio object state during the normal operation of an application—instead, use methods directly on audio object types.